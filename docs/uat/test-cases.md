# AIBoard 受け入れ試験(UAT)テストケース

実行: `python3 scripts/uat/run_uat.py`(自動分)。結果は `~/aiboard-private/uat/` に書く(実セッションの題名を含むためリポジトリに入れない)。
この表は `python3 scripts/uat/make_cases_doc.py` で試験コードから作る。

## 試験の約束

- 本番の盤(8791 番・`~/.aiboard`)には触れない。盤サーバは 8793 番・一時データ置き場で動かす
- 実セッションへは何も送らない。送信・終了・再開・端末移動は横取り(ブラウザ)か差し替え(アプリ)
- 「終了」の実シグナルは、試験が自分で起こした使い捨てプロセスにだけ送る。実セッションへは dry(送り先の解決だけ)
- 判定は画面の文字でなく、API・ps・会話ログの別実装の数え上げと突き合わせる

## 自動(155 件)

| ID | 区分 | 確認すること | 方法 |
|---|---|---|---|
| SV-01 | 盤サーバ(防御・起動・更新) | 盤サーバが試験用ポートで起動し、コードの版を返す | API を直接叩く |
| SV-02 | 盤サーバ(防御・起動・更新) | Host が自分以外なら 403 | API を直接叩く |
| SV-03 | 盤サーバ(防御・起動・更新) | 書き込み(POST)は X-Overview ヘッダーと自分の Origin が無ければ 403 | API を直接叩く |
| SV-04 | 盤サーバ(防御・起動・更新) | 盤のページに CSP(外部へ接続させない)が付く | API を直接叩く |
| SV-05 | 盤サーバ(防御・起動・更新) | コードが更新されたら、古いサーバを止めて入れ替える | API を直接叩く |
| SV-06 | 盤サーバ(防御・起動・更新) | 試験サーバが本番の PID ファイル(~/.aiboard/server.pid)を書き換えない | API を直接叩く |
| LP-01 | loop / skill / MCP の判定 | skill / MCP / 予約 / loop の集計が、会話ログを別実装で全件数えた結果と一致する | 関数を実データ・合成データで呼び、別実装の数え上げと照合 |
| LP-02 | loop / skill / MCP の判定 | 会話ログへの追記分だけを読み、書きかけの行は数えない | 関数を実データ・合成データで呼び、別実装の数え上げと照合 |
| LP-03 | loop / skill / MCP の判定 | cron の次の発火時刻 | 関数を実データ・合成データで呼び、別実装の数え上げと照合 |
| LP-04 | loop / skill / MCP の判定 | /loop 待機中は「あなたの番」に数えない。判断待ちは loop 中でも数える | 関数を実データ・合成データで呼び、別実装の数え上げと照合 |
| LP-05 | loop / skill / MCP の判定 | loop が止められた・次の起床から 15 分以上過ぎたら、ループ中と出さない | 関数を実データ・合成データで呼び、別実装の数え上げと照合 |
| LM-01 | 上限 | 上限の解除時刻の読み取り(3 形式) | 関数を呼ぶ・実ログと照合 |
| LM-02 | 上限 | 実際の会話ログにある上限の記録を検出する | 関数を呼ぶ・実ログと照合 |
| HK-01 | hook の設置 | hook の設置: 既存の hook を残す・控えを取る・外すと自分の分だけ消える・壊れた JSON は触らない | 一時の settings.json に対して実行 |
| ST-03 | 設定 | skill 数と MCP の接続状態が、独立に数えた値と一致する | 画面と API と CLI(claude mcp list)を照合 |
| MM-04 | メモリ・止める | 「終了」の送り先: 実セッションに対し試しのみ(dry)で、tty 上の claude/codex 本体 1 つに解決する | 画面操作(送信は横取り)・dry・使い捨てプロセス |
| MM-05 | メモリ・止める | 「終了」の実シグナル: 使い捨てプロセスに SIGTERM を送り、終わったことを確かめる | 画面操作(送信は横取り)・dry・使い捨てプロセス |
| I18N-01 | 言語 | 盤の固定文言(T('…'))すべてに英語訳がある | ソースの静的検査 |
| BD-01 | 盤の表示 | 盤を開いてスクリプトエラーが出ない(日本語・英語・デモ) | Playwright で画面を開き API と照合 |
| BD-02 | 盤の表示 | 「いま」のカードの数・状態の色が API の中身と一致する | Playwright で画面を開き API と照合 |
| BD-03 | 盤の表示 | 上のバーの数(あなたの番・作業中・メモリ合計)が API と一致する | Playwright で画面を開き API と照合 |
| BD-04 | 盤の表示 | 上のバーが 1024px 幅でもはみ出さない | Playwright で画面を開き API と照合 |
| BD-05 | 盤の表示 | 英語表示の固定文言に日本語が残っていない(利用者の依頼文は除く) | Playwright で画面を開き API と照合 |
| BD-06 | 盤の表示 | デモモードで、本物の顧客名・フォルダ名・ユーザー名・MCP 名が画面に出ない | Playwright で画面を開き API と照合 |
| BD-07 | 盤の表示 | 「過去」に切り替えると 30 日分のカードが出る | Playwright で画面を開き API と照合 |
| BD-08 | 盤の表示 | 検索で絞ると、該当カードが残り他は薄くなる/消える | Playwright で画面を開き API と照合 |
| CV-01 | 会話ビュー | カードを押すと会話ビューが開き、会話が表示される | Playwright(送信は横取り) |
| CV-02 | 会話ビュー | 日本語変換の確定 Enter では送らず、通常の Enter では 1 回だけ送る(送信は横取り) | Playwright(送信は横取り) |
| MM-01 | メモリ・止める | メモリ一覧: 多い順に並び、合計が一致する | 画面操作(送信は横取り)・dry・使い捨てプロセス |
| MM-02 | メモリ・止める | 「終了」は 1 回目では送らず、2 回目(4 秒以内)で 1 回だけ送る。4 秒過ぎると戻る | 画面操作(送信は横取り)・dry・使い捨てプロセス |
| MM-03 | メモリ・止める | デモモードでは「終了」を押しても何も送らない | 画面操作(送信は横取り)・dry・使い捨てプロセス |
| ST-01 | 設定 | 設定パネル: アカウント・hook・束ね方・skill と MCP・盤の 5 区画が出て、アカウント数が API と一致 | 画面と API と CLI(claude mcp list)を照合 |
| LP-06 | loop / skill / MCP の判定 | ループ待機のカードは青で「次 HH:MM」を出す(応答を差し替えて確認) | 関数を実データ・合成データで呼び、別実装の数え上げと照合 |
| AP-01 | アプリ(macOS) | アプリがビルドできる(release) | ビルドしたアプリを試験用ポートで起動し、ページ内で JS を実行(送信は差し替え) |
| AP-02 | アプリ(macOS) | アプリ内の確認ダイアログ: はい/いいえ・入力が JS に正しく返る(以前は黙って「いいえ」) | ビルドしたアプリを試験用ポートで起動し、ページ内で JS を実行(送信は差し替え) |
| AP-03 | アプリ(macOS) | アプリ内では、会話ビューの端末ボタンは常に「右の端末で開く」(iTerm を呼ばない) | ビルドしたアプリを試験用ポートで起動し、ページ内で JS を実行(送信は差し替え) |
| AP-04 | アプリ(macOS) | 別の端末で動くセッションを右の端末で開く: 止まっていれば 終了→右で再開。作業中は確認→Esc→止まらなければ終了しない(送信は差し替え) | ビルドしたアプリを試験用ポートで起動し、ページ内で JS を実行(送信は差し替え) |
| AP-05 | アプリ(macOS) | 起動すると右に端末が 1 枚開き、「右の端末で開く」の後はキーボードの入力先が端末になる | ビルドしたアプリを試験用ポートで起動し、ページ内で JS を実行(送信は差し替え) |
| AP-06 | アプリ(macOS) | 右の端末が生きていて打てる: 起動した端末に文字を送ると、その中のシェルが実行する | ビルドしたアプリを試験用ポートで起動し、ページ内で JS を実行(送信は差し替え) |
| RD-01 | RD | 秘密の伏せ字: 主な鍵の形をすべて隠し、普通の文は壊さず、二度通しても変わらない |  |
| LM-03 | 上限 | 上限の判定: 依頼だけでは解けず、本物の返答が来たら解ける(合成ログ 4 通り) | 関数を呼ぶ・実ログと照合 |
| LM-04 | 上限 | 上限の有効判定: 解除時刻が過ぎていれば解除・時刻が無ければ 5 時間窓・Codex の文面を読む | 関数を呼ぶ・実ログと照合 |
| AT-01 | AT | 「あなたの番」の並び: 確認待ち→長い放置→起動待ちの順で、対象でない状態は出さない |  |
| GR-01 | GR | 顧客/プロジェクトの束ね: 生きた全タブを一度だけ入れ、集計と並びが手計算と一致する |  |
| CS-02 | CS | 端末の指し先は tty だけ: 形の違う tty では AppleScript を組み立てず iTerm に一切触らない |  |
| CS-09 | CS | 依頼文の取り出し: メタ・system 注入・サブエージェント・ツール結果を「あなたの依頼」に混ぜない |  |
| CS-10 | CS | 最初の依頼と最後の依頼が同じ規則で拾われる(空白の入った JSON・大きなログでも) |  |
| SV-07 | 盤サーバ(防御・起動・更新) | すべての経路がガードの内側にあり、POST は副作用の前に弾かれる | API を直接叩く |
| SV-15 | 盤サーバ(防御・起動・更新) | 生きているかの判定は、そのポートで待ち受けている盤サーバ本体だけを見る | API を直接叩く |
| PK-01 | PK | 出荷する .app: 実行ファイル・Info.plist・アイコン・同梱 board が揃い、署名が通り、手元のコードと一致する |  |
| PK-06 | PK | README と LP の機能の記述が実装にある(端末エンジン名・ショートカット・参照ファイル) |  |
| PK-08 | PK | 公開物(リポジトリのテキスト・LP の画像と動画・収録スクリプト)に顧客名・利用者名・実フォルダが出ない |  |
| CS-12 | CS | 一覧の桁: 日本語を切り詰めても列の幅がぴったり揃う |  |
| TR-01 | TR | 使い方の案内は最後まで進んで閉じ、英語表示では日本語が出ず、吹き出しは画面内に収まる |  |
| AP-13 | アプリ(macOS) | 終了時: 盤が教えた sid ごと端末一覧を state.json に残し、app_panes.json は消す(tty: の擬似 sid は入れない) | ビルドしたアプリを試験用ポートで起動し、ページ内で JS を実行(送信は差し替え) |
| SV-10 | 盤サーバ(防御・起動・更新) | 送信の宛先ガード: 入れ替わり・素のシェル・無いタブ・許可外キーでは端末へ一切送らない | API を直接叩く |
| SV-11 | 盤サーバ(防御・起動・更新) | 送信の中身: tty 宛に文字コードで渡し(文字列を混ぜない)、Enter の有無と特殊キーを取り違えない | API を直接叩く |
| SV-12 | 盤サーバ(防御・起動・更新) | 「終了」の相手の絞り込み: 親子なら親 1 つ・同じ tty に 2 系統あれば止めない(試しのみ) | API を直接叩く |
| CV-03 | 会話ビュー | 会話ビューの承認ボタン(1 / 2 / Esc)は、表示中の tab と sid へ 1 回だけ正しい形で送る | Playwright(送信は横取り) |
| BD-13 | 盤の表示 | カードの状態色は 1 つだけで、上限 > 判断待ち > ループ待機 > あなたの番 > 作業中 の順で決まる | Playwright で画面を開き API と照合 |
| CS-04 | CS | AI の判定: 名前に codex / claude を含むだけのコマンドを AI のセッションと見なさない |  |
| CS-01 | CS | アプリの端末台帳: 生きているアプリの分だけ取り込み、死亡・破損・欠損では 0 枠(落ちない) |  |
| CS-05 | CS | セッションの本体 pid: 同じ tty の入れ子の AI でなく外側の 1 本を選ぶ(番号の小ささで選ばない) |  |
| CS-06 | CS | 状態の真理値表: 作業中・返答待ち・確認待ち・起動中?・確認画面で停止(画面/設定の両方)・codex 4 種・他のジョブ・終了 |  |
| TU-02 | TU | 信頼の確認で止まったセッションを、盤から答えられる(はい=↓+Enter・いいえ=Enter) |  |
| NT-02 | NT | 通知が本当に macOS に届く: 許可の状態を見て 1 通出し、配信を確認して取り下げ、押した先(端末)まで進む |  |
| NT-03 | NT | 通知が止められていたら盤が知らせる: 許可の状態を snapshot に出し、押すと設定画面を開くよう頼む |  |
| AP-16 | アプリ(macOS) | 起動直後にそのまま打てる(入力先が端末)。カードの「右の端末で開く」でその端末に移る | ビルドしたアプリを試験用ポートで起動し、ページ内で JS を実行(送信は差し替え) |
| SV-19 | 盤サーバ(防御・起動・更新) | アプリを入れ直したら、アプリの起動で古い盤サーバが新しい版に入れ替わる | API を直接叩く |
| TU-01 | TU | フォルダの信頼: 設定にある答えだけを信頼済みと読む(全アカウント分・壊れた設定は無視・末尾の / は同一視) |  |
| CS-03 | CS | メモリ合計: 実プロセス表で別実装と一致し、親子が循環した表でも止まる |  |
| CS-07 | CS | codex の記録の選び方: 実際に開いているファイル(lsof)が推定より優先され、別フォルダの記録は選ばない |  |
| CS-08 | CS | codex の記録の読み取り: model・依頼・いまの操作・ターンの区切り。壊れた行で落ちない |  |
| CS-11 | CS | ファイル単位のキャッシュ: 変われば読み直し・ファイルごとに分かれ・何度変わっても辞書が増えない |  |
| RD-02 | RD | 秘密はセッション情報のどの欄にも出ない(合成タブで全欄を走査) |  |
| SN-01 | SN | snapshot の内部整合: JSON にでき、件数・あなたの番・束ねが sessions と矛盾しない |  |
| MC-01 | MC | メモリの内訳: どのプロセスも 1 区分だけに数え、AI は claude/codex の子孫まで含める |  |
| JS-01 | JS | 強制終了(Jetsam)の集計: 今日/24 時間を正しく数え、壊れたファイルは件数に混ぜず ok=False にする |  |
| RM-01 | RM | 遠隔ジョブ: 60 秒は使い回し、失敗したら古い値を今の値として返さない(ssh は起こさない) |  |
| EX-01 | EX | 拡張の一覧に MCP の鍵・URL・起動コマンドが出ない(名前と種類だけ) |  |
| TR-02 | TR | 今日の依頼件数: Codex の記録も数え、4MB を超える Claude ログでは「下限値」の印を付ける |  |
| HK-02 | hook の設置 | hook が 5 つの合図すべてに、実在するファイルを指すコマンドとして、Claude を待たせない設定で入る | 一時の settings.json に対して実行 |
| HK-03 | hook の設置 | 2 度目の設置は 1 バイトも変えず、控えも増やさない。権限と他の設定は元のまま | 一時の settings.json に対して実行 |
| HK-04 | hook の設置 | どれが自分の hook かを取り違えない: 古い置き場は今の場所へ・入口(symlink)経由でも二重にせず・別人の同名ファイルは触らない | 一時の settings.json に対して実行 |
| HK-05 | hook の設置 | 合図ごとに「いま何をしているか」と題名の印が決まった値になる(cs が読む契約) | 一時の settings.json に対して実行 |
| HK-06 | hook の設置 | 状態ファイルには cs が読む項目が揃い、渡された余分な値は書き込まない | 一時の settings.json に対して実行 |
| HK-07 | hook の設置 | サブエージェントの操作で親の「いま何をしているか」を上書きしない。10 分で消える | 一時の settings.json に対して実行 |
| HK-08 | hook の設置 | 壊れた入力・欠けた入力でも exit 0 で、状態を壊さず、失敗は黙らずログに残る | 一時の settings.json に対して実行 |
| HK-09 | hook の設置 | 同じセッションに 12 個同時に来ても、状態ファイルはいつでも読めて残骸も残らない | 一時の settings.json に対して実行 |
| HK-10 | hook の設置 | 会話記録が 1GB でもフックは末尾だけ読んで一瞬で終わる(Claude を待たせない) | 一時の settings.json に対して実行 |
| HK-11 | hook の設置 | 起動の合図で片付くのは 2 日より古い状態ファイルだけ(他人のファイルやログは消さない) | 一時の settings.json に対して実行 |
| HK-12 | hook の設置 | 端末の解決と、タブへ出す色・透かし・題名の中身 | 一時の settings.json に対して実行 |
| HK-13 | hook の設置 | 顧客の作業はその顧客の色と名前で出て、後の合図でも忘れない | 一時の settings.json に対して実行 |
| IX-01 | IX | 合成した会話ログが、作ったとおりに索引される(数えてはいけない行を数えない) |  |
| IX-02 | IX | 増分索引: 変わっていなければ読み直さず、追記した1本だけ読み直し、消えた記録は索引から消える |  |
| IX-03 | IX | 種別(role)の割り振り: サブ・査読・補助・無人実行・人の会話が根拠つきで1つに決まる |  |
| IX-04 | IX | 「つづき」は根拠のある相手にだけ引く(直前の別件には引かない・根拠が無ければ引かない) |  |
| IX-05 | IX | 表示の絞り込み(期間・無人実行・子・動作中)が効き、上のバーの件数は絞り込みで変わらない |  |
| IX-06 | IX | 期間外でも「つづきの相手」は ghost として出し、エッジは両端が揃っている時だけ返す |  |
| IX-07 | IX | Codex の状態DBは読むだけ。期間より前の Codex は索引に複写しない |  |
| IX-08 | IX | 壊れた記録が 1 本あっても索引全体は作られる(その1本だけ諦める) |  |
| IX-09 | IX | 子プロセスの取り出し(--query)が JSON を返し、API キーが題名にも依頼文にも残らない |  |
| IX-10 | IX | 索引の保存は変わった行だけ書き、消えた行は消す(他の行を書き潰さない) |  |
| IX-11 | IX | 索引に書き込んでいる最中でも、盤の取り出しは待たされない(WAL) |  |
| IX-12 | IX | 「同じファイル」の線は 7 日以内の別セッション同士だけ(親子・多すぎ・重複は引かない) |  |
| BD-09 | 盤の表示 | 再描画でカードの位置と DOM 要素が変わらず、増えたら足され、消えたら残らない | Playwright で画面を開き API と照合 |
| BD-10 | 盤の表示 | 引き(縮小)では状態の色の面だけになり、戻すと文字が読める | Playwright で画面を開き API と照合 |
| BD-11 | 盤の表示 | 検索欄に打った f / 0 / + / - では盤が動かない。欄の外では効く | Playwright で画面を開き API と照合 |
| BD-12 | 盤の表示 | 動いている AI が無くなったらカードは残らず、案内が出て、上のバーが 0 になる | Playwright で画面を開き API と照合 |
| BD-14 | 盤の表示 | 同じフォルダのカードは同じ枠に入り、枠の中で重ならない(デモでも分かれ方は同じ) | Playwright で画面を開き API と照合 |
| BD-15 | 盤の表示 | 欠けた値・仕込まれた HTML のセッションでも、盤は落ちず 1 枚も落とさない | Playwright で画面を開き API と照合 |
| CV-04 | 会話ビュー | 会話ビューは 依頼/返答/操作 を順に並べ、連続する操作を畳み、記録中の HTML を実行しない | Playwright(送信は横取り) |
| RL-01 | RL | 「過去」の上の列は 判断待ち→返答済み→作業中 の順で、押すとそのカードが選ばれる(「いま」では出さない) |  |
| SV-08 | 盤サーバ(防御・起動・更新) | 入力の形が違う要求は、iTerm を触る前に 400/404/409 で断る | API を直接叩く |
| SV-09 | 盤サーバ(防御・起動・更新) | 壊れた値・長すぎる本文でもサーバは落ちず、必ず JSON で断り、記録も残さない | API を直接叩く |
| SV-13 | 盤サーバ(防御・起動・更新) | 設定とアカウントの使い回し: 期限内は CLI を呼び直さず、期限切れと refresh=1 では呼び直す | API を直接叩く |
| SV-14 | 盤サーバ(防御・起動・更新) | 会話の取り直し: 記録が変わらなければ作り直さず、1 行増えたら作り直す | API を直接叩く |
| SV-16 | 盤サーバ(防御・起動・更新) | 索引の問い合わせ: 条件をそのまま子プロセスに渡し、子が失敗しても 500 を返してサーバは生き続ける | API を直接叩く |
| SV-17 | 盤サーバ(防御・起動・更新) | 同時に来ても待たせない: 調べ直しは裏で 1 回、応答は使い回す | API を直接叩く |
| SV-18 | 盤サーバ(防御・起動・更新) | 盤の中身は裏で更新され続ける(固まったものを返し続けない) | API を直接叩く |
| AP-07 | アプリ(macOS) | 端末の作業フォルダ: 空白と ' を含むフォルダでも端末が生き、その場所で始まる。無いフォルダはホームに落ちる | ビルドしたアプリを試験用ポートで起動し、ページ内で JS を実行(送信は差し替え) |
| AP-08 | アプリ(macOS) | 端末台帳(app_panes.json)の tty と pid が ps の実プロセスと一致し、端末ごとに別の tty。終了で台帳を消す | ビルドしたアプリを試験用ポートで起動し、ページ内で JS を実行(送信は差し替え) |
| AP-09 | アプリ(macOS) | send の宛先: 指定した端末だけに届く(選択中の端末に流れない)・4000 字超は送らない・知らないタブは無視 | ビルドしたアプリを試験用ポートで起動し、ページ内で JS を実行(送信は差し替え) |
| AP-10 | アプリ(macOS) | 盤からの指示の検査: 形の違う resume/run/open/switchAccount は端末を開かない。決まった形の run だけ通る | ビルドしたアプリを試験用ポートで起動し、ページ内で JS を実行(送信は差し替え) |
| AP-11 | アプリ(macOS) | 前回の端末の復元: sid があればその会話、無ければ一覧から選ばせる(勝手に最新を開かない) | ビルドしたアプリを試験用ポートで起動し、ページ内で JS を実行(送信は差し替え) |
| AP-12 | アプリ(macOS) | 別アカウントで続き: 記録を相手の置き場へコピーし、CLAUDE_CONFIG_DIR 付きの command claude で再開する | ビルドしたアプリを試験用ポートで起動し、ページ内で JS を実行(送信は差し替え) |
| AP-14 | アプリ(macOS) | 見張り: 起動時の状態では通知せず、変わった時だけ 1 回。判断待ちの数が Dock の数字になる | ビルドしたアプリを試験用ポートで起動し、ページ内で JS を実行(送信は差し替え) |
| AP-15 | アプリ(macOS) | メニュー: 短縮キーが重ならず、押した先の処理が実在し、英語表示に日本語が混ざらない | ビルドしたアプリを試験用ポートで起動し、ページ内で JS を実行(送信は差し替え) |
| PK-02 | PK | 同梱した board だけを別の場所へ写しても盤サーバが起動し、同梱の overview.html を CSP 付きで配る |  |
| PK-03 | PK | make_app.sh: swift build が失敗したら、古い実行ファイルを包んで配らない |  |
| PK-04 | PK | アイコン: icns の 10 寸法が make_icon.py の出力と画素一致し、LP のファビコンも同じ生成物 |  |
| PK-05 | PK | 外部送信ゼロの証明: 外への接続を見つける・何も測れていない時に「0 件」と言わない |  |
| PK-07 | PK | README と LP の導線が出荷物で裏づけられている(未置換の URL・brew・MIT・依存ライセンス) |  |
| PK-09 | PK | LP と README が指すデモ動画・画像が実物と一致する(尺・寸法・音声なし・宣言した縦横比) |  |
| WR-01 | WR | 持ち場の判定: 触ったファイルから置き場を1つ選び、道具置き場や1段では足りない所を取り違えない |  |
| WR-02 | WR | ホームで動く会話も、触ったファイルの置き場ごとに別の枠に分かれる(デモでは実名を出さない) |  |
| WR-03 | WR | 過去の記録にも同じ規則で持ち場が付き、cwd で分かる会話には付けない |  |
| GP-01 | GP | 束ね方の上書き: 保存できる形だけ受け取り、壊れた値は 400 で断る(本番の設定は触らない) |  |
| GP-02 | GP | 上書きした名前・色・顧客が、盤の枠とカードに出る |  |
| GP-03 | GP | AI の返事の取り込み: JSON を表に入れるだけで、保存も外部送信もしない |  |
| BD-16 | 盤の表示 | 小さい窓では枠を畳んで一覧にする: 45% を下回らず、急ぎの枠は開いたまま、押せば開く | Playwright で画面を開き API と照合 |
| PJ-01 | PJ | 案件の共通指示: 保存でき、Claude は system prompt・Codex は最初のメッセージとして渡る(実行はしない) |  |
| OF-01 | OF | 公式の状態源(claude agents --json): 状態の言い換えと、hook が無い時だけ補うこと |  |
| DG-01 | DG | まとめ役の選び方: 上限のアカウントを避け、全部上限なら投げない |  |
| DG-02 | DG | 任せる: 選ばれた AI とアカウントで端末を起こし、共通の指示と依頼文を渡す(実行はしない) |  |
| NT-01 | NT | 申し送り: 保存と読み出し、長すぎる入力は断る、他の案件を壊さない |  |
| PJ-02 | PJ | 申し送りは端末にも渡る: 共通の指示のあとに続けて渡す(実行はしない) |  |
| TL-01 | TL | 案件パネル: 指示・申し送り・その案件のセッションだけが時系列で出て、押すとそのカードが選ばれる |  |
| IT-01 | IT | iTerm が固まっても盤は止まらない: 時間切れであきらめ、前の一覧を使い、間を空けて再挑戦し、画面に出す |  |
| AC-02 | AC | アカウント: ログイン状態とプランは CLI を正とし、設定ファイルの古い値で上書きしない |  |
| AC-03 | AC | この Mac の AI CLI 一覧: 入っているか・ログインしているか・鍵は読まない |  |
| AC-04 | AC | 他の AI(Gemini/Grok/Cursor)の端末も盤に出る(状態は不明と出す) |  |
| RL-02 | RL | 配布物: dmg と zip から取り出した .app が動く形で、チェックサムが一致する |  |
| RL-03 | RL | 配る手順: 署名が無いときは Releases も brew も未署名を配らない形になっている(静的検査) |  |
| RE-01 | RE | 本物のセッションに、盤から送って返事が返り、盤から止められる(使い捨てのセッションを自分で作る) |  |

## 手動(6 件・実機で本人が確認)

自動で確かめられないもの(実際の日本語入力・通知の配信・キーボードの入力先・実セッションへの送信と終了・実ログインでのアカウント切替)。

| ID | 手順と期待 |
|---|---|
| MA-01 | 日本語入力: アプリの会話ビューで「てすと」→変換→Enter で確定しても送られない。もう一度 Enter で送られる |
| MA-02 | 通知を押す: 出た通知をクリックすると、その端末が前に出る(出す・届く・押した先の処理は NT-02 で自動) |
| MA-03 | 判断待ちのボタン: 実セッションが許可を求めた時、会話ビューの 1 / 2 / Esc が効く(RE-02 待ち) |
| MA-05 | 終了: 不要なセッションをメモリ一覧で 2 回押しして終了。端末は残り、盤から消え、「過去」から再開できる |
| MA-06 | 別アカウントで続き: 上限に当たったセッションで他アカウントのボタンを押し、続きが開く(ログイン済みアカウントで) |
| MA-09 | 右の端末で開く(別の端末で動いていた会話): 押すと元の端末の AI が終わり、右の端末で同じ会話が続きから開いて、そのまま打てる |
