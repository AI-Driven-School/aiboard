# 実際の使い方と、製品がそれに耐えるか（2026-09-18 実測）

数字はすべて、この Mac の Claude Code / Codex の記録から測ったもの。取り出しは盤の索引 API（`/api/index?days=30`）と `board/overview_index.py` の関数。DB は複製せず読み取りのみ。

## 使い方の実態（直近 30 日）

| 測ったこと | 数字 |
|---|---|
| 索引にある記録 | **13,430 件**（人の会話 321・サブエージェント 6,090・無人実行 6,400・Codex 729） |
| 無人実行の内訳 | `claude -p` / SDK が **6,016 件**。残りは定型の1回だけの指示 |
| 同時に動いていた最大数 | 全体 **50**（8/30 19:11）／人の会話だけで **25**（9/15 11:38） |
| 1 日の本数 | 人の会話 中央値 **6**・最大 **35**／全体 中央値 258・最大 2,405 |
| 1 セッションの長さ | 中央値 **10.8 時間**・p90 93 時間・最大 **332 時間**（開きっぱなし） |
| 1 セッションの中身 | 依頼 中央値 9・p90 53・最大 388／道具 中央値 131・p90 758・**最大 4,996** |
| 子（サブエージェント）を持つ会話 | **24.9%**（最大 2,420 人） |
| 会話ログの大きさ | 中央値 4.7MB・p90 54MB・**最大 171MB**（4MB 超が 173 件） |
| 動かす場所 | **`~` が 263/321 = 82%**。残りは 5 つのフォルダに分かれ、最大でも 23 件 |
| 顧客が付く割合 | **21%**（68/321。4 社に分かれ、最大の 1 社が 31 件・最小が 8 件） |
| モデル | Opus 5 が 7,644・Sonnet 4.x 851・Sonnet 5 655・GPT-6 Astra 570・Haiku 191・Fable 187 |
| アカウント | 既定 13,355・som 75 |

## 耐えるか（実測して確かめた）

| 見たこと | 結果 | 数字 |
|---|---|---|
| 「いま」の表示 | ◎ | カード 11 枚・1 フレーム 8.4ms・JS ヒープ 10MB |
| 「過去」の既定 | ◎ | 148 枚が **3.2 秒**で落ち着く（索引 API 335 件 0.7MB 2.3 秒） |
| 「過去」＋無人実行 | △ | **5,904 枚**が 5.8 秒・1 フレーム **35ms**（約 28fps）・JS ヒープ 33〜78MB。既定では隠しているので普段は当たらない |
| 検索 | ◎ | 5,904 枚から 16 枚へ 1.7 秒 |
| 大きい会話ログ | ◎ | 171MB で 道具の集計 初回 0.31 秒・2 回目 0.000 秒／今日の件数 0.01 秒／最後の依頼 0.00 秒／上限 0.00 秒 |
| 同時 25 本 | ◎（推定） | いまは 11〜13 本で 0.37 秒。索引側は 13,430 件で 2.3 秒 |

## 合っていなかったこと → 直したこと

1. **束ね方が実態に合っていない（最大の不一致）**
   盤は「顧客 → プロジェクト（cwd のフォルダ名）」で束ねる作りだが、**人の会話の 82% は `~` で動いている**ので、ほぼ全部が 1 つの「自社・その他」枠に入り、しかも直近 20 件で打ち切られていた。顧客が付くのは 21% だけ。
   → **触っているファイルの置き場で持ち場を決める**ようにした（`overview.work_root` / `project_hint`、索引側は `_hint_from_files`）。いま動いている 10 件のうち **8 件**に持ち場が付き（自社の作業フォルダ 5 種に分かれた）、過去 30 日でも **97 件**が名前付きの枠に分かれる。`~/Desktop/ECX` のように 1 段では足りない所は 2 段見る。道具置き場（`.claude` など）と、直下に置いただけのファイルは持ち場にしない。試験: WR-01 / WR-02 / WR-03。

2. **長い依頼を送ると行が混ざる**
   3,500 字を送ると、貼り付けが終わる前に Enter が飛んで行が途中で切れていた。依頼の中央値は 9 回・p90 53 回で、長文を貼る使い方は日常。
   → 端末ごとに **1 件ずつ順番に**送り、Enter は貼り付けの長さに応じて待つ（最大 0.8 秒）。試験: AP-09。

3. **入れ子の AI を本体と取り違える**
   1 セッションが道具を最大 4,996 回使い、24.9% が子を持つ。子として `claude -p` を起こすと、pid が一周した後では**内側の一時実行をセッション本体と誤認**していた（メモリ・状態・「終了」の宛先が全部ずれる）。
   → 他の AI の子孫でない「外側」を選ぶ（`cs.outermost_ai`）。試験: CS-05。

4. **`~/logs/codex` のようなファイルを Codex セッションと誤判定**
   → コマンド行の先頭（node 経由なら 2 つ目）だけを見る。試験: CS-04。

5. **壊れた 1 行で索引が全部落ちる／題名に API キーが残る**
   会話ログは 13,430 本もあり、1 本でも壊れれば 30 日分の索引が作れなくなっていた。
   → `prompt_text` を壊れた行に強くし、題名も伏せ字の対象にした。試験: IX-08 / IX-09。

## まだ耐えていないこと（判断が要る）

- **無人実行を表示に入れると重い**: 5,904 枚で 1 フレーム 35ms。既定は非表示なので普段は当たらないが、`claude -p` を 1 日 200 本走らせる使い方（実測 6,016 件/30 日）では、表示したときに引っかかる。案: 無人実行は日付ごとに畳んで 1 枚にする（枚数を 30 分の 1 に）。
- **332 時間続くセッション**: 「終了」の判定と `ago` 表示はできているが、長時間開きっぱなしの会話ログは 171MB まで育つ。いまは末尾読みで 0.01 秒だが、索引の再構築では毎回全部読む。
- **顧客の色分けが 21% にしか付かない**: いまはフォルダと語句で判定している。持ち場（項目 1）が付いたので、**持ち場 → 顧客**の対応表を持てば増やせる。
- **同時 25 本の実機確認は未実施**: 測ったのは 11〜13 本のとき。25 本並んだ状態での通知・Dock の数字・盤の反応は、次にその状況が起きたときに測る。
